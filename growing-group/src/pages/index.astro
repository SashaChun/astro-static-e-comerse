---
import ProductGrid from "../components/ProductGrid.astro"
import MainLayout from "../layouts/MainLayout.astro"
---

<MainLayout>
    <main>
        <section class="container mx-auto px-6 py-20">
            <div class="grid md:grid-cols-2 gap-12 items-center">
                <div class="space-y-6 animate-fade-in">
                    <h1 class="text-6xl md:text-7xl font-bold leading-tight">
                           <span class="bg-gradient-to-r from-purple-600 via-pink-600 to-blue-600 bg-clip-text text-transparent">
                               привіт світ
                           </span>
                        <br>
                        <span class="text-gray-800">2026</span>
                    </h1>
                    <p class="text-xl text-gray-600 leading-relaxed">
                        Відкрийте для себе найкращі товари сезону з неймовірними знижками
                    </p>
                    <button class="bg-gradient-to-r from-purple-600 to-pink-600 text-white px-8 py-4 rounded-full font-semibold text-lg shadow-lg hover:shadow-2xl hover:scale-105 transition-all duration-300 flex items-center gap-2">
                        Переглянути каталог
                        <span class="text-2xl">→</span>
                    </button>
                </div>
                <div class="relative">
                    <div class="w-full h-96 bg-gradient-to-br from-purple-400 via-pink-400 to-blue-400 rounded-3xl shadow-2xl transform rotate-3 hover:rotate-6 transition-transform duration-500"></div>
                    <div class="absolute inset-0 w-full h-96 bg-gradient-to-br from-blue-400 via-purple-400 to-pink-400 rounded-3xl shadow-2xl transform -rotate-3 hover:-rotate-6 transition-transform duration-500"></div>
                </div>
            </div>
        </section>

        <section class="container mx-auto px-6 py-20">
            <h2 class="text-5xl font-bold text-center mb-16 bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                Популярні товари
            </h2>
            <ProductGrid/>
        </section>

        <section class="bg-gradient-to-r from-purple-600 via-pink-600 to-blue-600 py-20 mt-20">
            <div class="container mx-auto px-6 text-center text-white space-y-6">
                <h2 class="text-5xl font-bold">Підпишіться на розсилку</h2>
                <p class="text-xl opacity-90">Отримуйте ексклюзивні пропозиції та знижки до 50%</p>
                <div class="flex gap-4 max-w-md mx-auto">
                    <input type="email" placeholder="Ваш email" class="flex-1 px-6 py-4 rounded-full text-gray-800 focus:outline-none focus:ring-4 focus:ring-white/50 transition-all">
                    <button class="bg-white text-purple-600 px-8 py-4 rounded-full font-bold hover:bg-gray-100 hover:scale-105 transition-all duration-300 shadow-lg">
                        Підписатися
                    </button>
                </div>
            </div>
        </section>

    </main>

</MainLayout>