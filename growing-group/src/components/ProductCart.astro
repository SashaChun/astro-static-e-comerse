---
const { name, price, oldPrice } = Astro.props;
---

<div class="p-6 space-y-3">
    <h3 class="text-xl font-bold text-gray-800 group-hover:text-purple-600 transition-colors">
        {name}
    </h3>

    <p class="text-gray-600">
        Опис товару з основними характеристиками
    </p>

    <div class="flex items-center justify-between">
        <div>
      <span class="text-2xl font-bold text-purple-600">
        ₴{price}
      </span>

            {oldPrice && (
                    <span class="text-gray-400 line-through ml-2">
          ₴{oldPrice}
        </span>
            )}
        </div>

        <button class="bg-purple-600 text-white p-3 rounded-full hover:bg-pink-600 transition-colors duration-300 hover:scale-110 transform">
            🛒
        </button>
    </div>
</div>
