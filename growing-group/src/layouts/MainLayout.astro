---
import "../styles/global.css"

const pathname = Astro.url.pathname;

const getNavClass = (path: string) => {
  const isActiveLink = path === "/" ? pathname === path : pathname.startsWith(path);
  return `relative ${isActiveLink ? "text-purple-600" : "text-gray-700"}`;
};

---

<html lang="uk">
<head>
    <meta charset="UTF-8" />
    <meta name="viewport" content="width=device-width" />
    <meta name="description" content="Сучасний інтернет-магазин з якісними товарами та швидкою доставкою по Україні. Великий вибір продукції за вигідними цінами з гарантією якості" />
    <link rel="icon" type="image/svg+xml" href="/favicon.svg" />
    <link href="https://cdn.jsdelivr.net/npm/tailwindcss@3.3.0/dist/tailwind.min.css" rel="stylesheet">
    <title>Сучасний Магазин</title>
</head>
<body class="bg-gradient-to-br from-purple-50 via-pink-50 to-blue-50 min-h-screen">
<header class="bg-white/80 backdrop-blur-lg shadow-lg sticky top-0 z-50 border-b border-purple-100">
    <nav class="container mx-auto px-6 py-4">
        <div class="flex items-center justify-between">
            <div class="text-3xl font-bold bg-gradient-to-r from-purple-600 to-pink-600 bg-clip-text text-transparent">
                SHOP
            </div>
            <ul class="hidden md:flex space-x-8">

                <a href="/" class={getNavClass("/")} lang="uk">
                    Головна
                </a>
                <a href="/products/page/1" class={getNavClass("/products")} lang="uk">
                    Товари
                 </a>
                <a href="/about" class={getNavClass("/about")} lang="uk">
                    Про нас
                </a>

                <a href="/contacts" class={getNavClass("/contacts")} lang="uk">
                    Контакти
                </a>

            </ul>
            <div class="relative cursor-pointer group">
                <div class="bg-gradient-to-r from-purple-600 to-pink-600 text-white w-12 h-12 rounded-full flex items-center justify-center shadow-lg group-hover:shadow-xl group-hover:scale-110 transition-all duration-300">
                    <span class="text-xl">🛒</span>
                </div>
                <span class="absolute -top-2 -right-2 bg-red-500 text-white text-xs w-6 h-6 rounded-full flex items-center justify-center font-bold animate-pulse">3</span>
            </div>
        </div>
    </nav>
</header>
<slot/>
</body>
</html>